@model IEnumerable<LogisticaContainers.ModelFactories.ContainerCompleto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdContainer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionContainer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdEstadoContainer)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td> 
                @Html.DisplayFor(modelItem => item.IdContainer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionContainer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>

            <td>
                @Html.ActionLink("Edit", "Edit", new {  id=item.IdContainer  }) |
                    @Html.ActionLink("Details", "Details", new { id = item.IdContainer }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.IdContainer })
            </td>
        </tr>
}
    </tbody>
</table>
